server:
  port: 8108

spring:
  application:
    name: concert
  # H2 Database ??
  datasource:
    driver-class-name: org.h2.Driver
    url: 'jdbc:h2:mem:concert;MODE=MySQL;DATABASE_TO_LOWER=TRUE'   # H2 DB ?? ?? (In-Memory Mode)
    #url: 'jdbc:h2:~/test'    # H2 DB ?? ?? (Embedded Mode)
    username: lecture         # H2 DB ?? ID (??? ??)
    password: lecture         # H2 DB ?? PW (??? ??)

  # H2 Console ??
  h2:
    console: # H2 DB? ??? ??? ? ?? ??
      enabled: true           # H2 Console ?? ??
      path: /h2-console       # H2 Console ?? ??

  # JPA ??
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create        # DB ??? ?? (none, create, create-drop, update, validate)
    properties:
      hibernate:
        format_sql: true      # ?? ?? ?? (??)
        show_sql: true        # ?? ?? ??